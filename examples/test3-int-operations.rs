fn main() {
    let a = 8;
    let b = 4;
    assert!(a + b == 12);
    assert!(a - b == 4);
    assert!(a * b == 32);
    // assert!(a / b == 2);
    // assert!(a % b == 0);
    assert!(-a == -8);
}
